# MolManager 完整检查清单 · 核查结果

> 核查日期：2026-09-05 · 仓库 @ `a5f29d4` · 方法：代码级 grep/AST/实测验证
> 清单项总数：**130**（14 章）

---

## 一、先说定位错配

清单整体按**企业级多用户 Web 服务**设计（JWT、Redis、K8s、水平扩展、99.9% 可用性、GDPR、CSRF），
而 MolManager 是**单机桌面 Tkinter 应用**（无用户系统、无服务端、SQLite 本地库）。

粗略分布：

| 分类 | 项数 | 说明 |
|------|------|------|
| ⛔ **不适用**（架构层面无需）| ~55 | 多用户权限、分布式、Web 安全、合规 |
| ✅ **已满足** | ~50 | 注入防护、缓存、队列、Docker、CHANGELOG 等 |
| 🔴 **真缺口**（值得做）| ~15 | 见第三节 |
| 🟠 **需增强** | ~10 | 已有基础但不完整 |

---

## 二、⛔ 不适用项（建议从清单中裁掉）

| 清单章节 | 不适用原因 |
|---------|-----------|
| 二 · Redis 缓存层 / 水平扩展 / 微服务拆分 / gRPC | 桌面应用，已有 `utils/cache.py` 的 LRU 缓存，无需独立缓存服务 |
| 三 · 数据库分库分表 / RDKit Cartridge / Bingo | 单机 SQLite，桌面级数据量；化学搜索引擎是服务端方案 |
| 七 · API 密钥 / OAuth2 / 限流 | API 当前为**本机 localhost** 用途，无公网暴露面 |
| 八 · 响应式设计 / 移动端 / 无障碍 ARIA / 分子编辑器 | Tkinter 桌面 UI；Ketcher 等是 Web 组件（项目已有 4 处自绘结构相关代码）|
| 九 · 用户认证 JWT / 角色权限 / CSRF / XSS / HTTPS | 无用户系统、无 Web 前端、无网络传输 |
| 十 · 水平扩展 / 内存泄漏监控 / Prometheus+Grafana | 单机进程 |
| 十二 · K8s 编排 / ELK / 零停机部署 / 滚动更新 | Docker 已有，但编排属服务端运维 |
| 十四 · GDPR / 数据许可 / 受控物质报告 | 数据全在本地，无对外服务；**但开源许可证检查应保留** |

> 保留建议：第十四章的「开源组件许可证检查（Open Babel、Psi4）」**仍然适用且重要**——
> Open Babel 为 GPLv2，Psi4 为 LGPLv3，若将来做一键安装包分发需遵守其条款。

---

## 三、🔴 真缺口（按优先级）

### 🥇 P0 — 会真实破坏用户数据

| # | 清单项 | 代码证据 | 风险 |
|---|--------|---------|------|
| 1 | **数据库迁移机制**（三·10）| `storage_sqlite.py` 仅 `CREATE TABLE IF NOT EXISTS`，**`schema_version` 0 命中** | 🔥 **加字段不会自动应用到老库**。实测：`CREATE TABLE IF NOT EXISTS` 对已存在表加列 → `no such column: tags` 报错。**做「标签系统」前必须先解决，否则老用户升级即崩** |

### 🥈 P1 — 功能缺失

| # | 清单项 | 现状 |
|---|--------|------|
| 2 | **审计日志**（三·8、九·10）| 0 命中。无操作留痕（谁改了结构、谁提交了计算）|
| 3 | **API 版本控制** `/v1/`（七·7）| 端点为裸路径（`/health`、`/descriptors`），无版本前缀 |
| 4 | **API 认证**（七·9）| 0 命中。当前仅监听本机故风险低，但若未来开放需补 |
| 5 | **限流**（七·10）| 0 命中。同上 |
| 6 | **异步任务 ID**（七·8）| 仅 1 处（`_tabs.py`），`/calculate` 端点尚未建立任务模型 |

### 🥉 P2 — 需增强

| # | 清单项 | 现状 | 差距 |
|---|--------|------|------|
| 7 | **OB 线程安全**（五·5）| 有 `Lock`，但**只保护 CLI 路径单例初始化**，非 OB 对象操作 | 当前并发与 OB 未交叉（实测安全）；但 `utils/concurrency.py` 有通用线程池，未来若并行调用 OB 会出问题——**建议加文档警示** |
| 8 | 结构标准化（四·3）| 3 处命中 | 需确认是否覆盖去盐/中和/互变异构（当前仅见描述符相关）|
| 9 | 结构去重（四·7）| 6 处命中 | 需确认是否支持「忽略立体化学」「忽略同位素」三种粒度 |
| 10 | OpenAPI 文档（七·11）| FastAPI 自动生成 | 需补参数说明与化学格式示例 |

---

## 四、✅ 已满足的重点项（清单中较关键的）

| 清单项 | 证据 |
|--------|------|
| **防命令注入**（六·9、九·5）| 全部 `subprocess` 参数列表，`更新.py` 已去 `shell=True`，`psi4/core.py` 同 |
| **防 SQL 注入**（九·4）| 全部 `?` 参数化查询 |
| **健康检查端点**（十二·8）| `/health` 已实现，含依赖探测 |
| **Psi4 可重现性**（六·8）| 记录 Psi4 版本与计算参数（`psi4/core.py`）|
| **优雅关闭**（十二·9）| 4 处 `atexit`/`shutdown` 处理（`psi4/core.py`、`task_manager.py`）|
| **缓存**（十·4）| `LRUCache` + `TimedLRUCache`（`utils/cache.py`）、OB 专用缓存 |
| **任务队列**（二·3、六·4）| `core/task_manager.py` + `chem/psi4/_worker.py` |
| **第三方封装隔离**（二·6）| `chem/openbabel_utils/`（8 子模块）、`chem/psi4/`（8 模块）独立封装 |
| **统一异常处理**（二·8）| `api/server.py` 有全局 `exception_handler` |
| **Docker 容器化**（十二·1）| `Dockerfile` + `docker-entrypoint.sh` + `.dockerignore` |
| **CHANGELOG**（十三·8）| 已维护至 1.0.2 |
| **分页**（七·2）| 18 处命中 |
| **InChIKey / 分子式 / 分子量存储**（三·1）| 7 / 18 处命中 |
| **3D 坐标存储**（三·2）| `xyz_file` 字段 + conformer 模块（15 处）|
| **CI 流水线**（十一·10）| `.github/workflows/ci.yml`（ruff + mypy + pytest）|

---

## 五、🔗 关键依赖链：先迁移动，后标签

```
DB 迁移机制（P0）
        ↓ 阻塞
  标签系统改造
        ↓ 依赖
  tags 查询优化（关联表 / JSON1）
```

**理由**：`molecule` 表现有 `tags TEXT（JSON 数组）`字段。若要按审计结论改造成关联表或 JSON1 查询，
必然涉及 schema 变更。在**没有迁移机制**的情况下发布，老用户的数据库会直接报 `no such column`。

**最小可行迁移方案**（约 30 行）：
1. 用 `PRAGMA user_version` 存 schema 版本号
2. 写 `_MIGRATIONS = {1: "ALTER TABLE molecule ADD COLUMN tags TEXT", ...}`
3. 启动时比对版本，按序执行未应用的迁移
4. 迁移前自动备份数据库文件

---

## 六、建议的最终清单（裁剪后，共 25 项）

**P0（1 项）**
1. 数据库迁移机制 + 启动自动备份

**P1（5 项）**
2. 审计日志表与埋点
3. API 加 `/v1/` 前缀
4. API Key 认证（可选开关）
5. 限流（可选开关）
6. `/calculate` 异步任务模型 + 任务 ID

**P2（6 项）**
7. OB 线程安全文档警示
8. 结构标准化增强（去盐/中和/互变）
9. 去重粒度增强（立体/同位素）
10. OpenAPI 参数与示例补全
11. Open Babel(GPLv2) / Psi4(LGPLv3) 许可证合规检查
12. `/convert`、`/animate` 端点补齐

**回归保护（13 项）**
13. `core/model/*` 单元测试
14. `core/controller` 关键路径测试
15. 结构解析健壮性测试（无效 SMILES/InChI/Molfile）
16. OB 格式互转测试
17. Psi4 输入生成/输出解析测试
18. API 集成测试
19. 性能基准（搜索/转换/排队）
20. 恶意输入测试（超大分子、特殊元素）
21. 文件上传校验
22. 迁移回滚测试
23. 依赖漏洞扫描（CI）
24. 用户手册
25. 已知问题与限制记录

---

## 七、一句话结论

这份清单里**真正值得立刻动手的只有 1 项**：数据库迁移机制。它是后续所有数据模型改动
（标签系统、审计日志、去重字段）的前置依赖，也是唯一会真实破坏用户数据的缺口。
其余 P1/P2 项多是"锦上添花"，而清单中约 55 项因架构定位不同可直接裁掉。
