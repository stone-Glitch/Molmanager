# MolManager 升级指南 · 现状对照评估

> 生成日期：2026-09-05（基于仓库 `stone-Glitch/-` @ `a5f29d4` 代码实测核查）
> 目的：避免对已实现功能重复投入，聚焦真实缺口。

---

## 一、结论先说

对指南中 19 个可核查功能点做代码级核查，结果：

| 状态 | 数量 | 说明 |
|------|------|------|
| ✅ **已实现**（指南误列为待办）| **9 项** | 直接照做会重复造轮子 |
| ⚠️ **部分实现**（需增强）| **4 项** | 有基础但达不到指南描述 |
| ❌ **真实缺口** | **4 项** | 代码中零命中，确需从零做 |
| 🔶 **依赖已有设施** | **2 项** | 装好依赖即自动获得 |

---

## 二、✅ 已实现（不必再投入）

| 指南建议 | 优先级 | 实际现状 | 证据 |
|---------|--------|---------|------|
| **拖拽导入** | 高 | 已实现 | `core/drop_handler.py`，支持拖到 Treeview |
| **递归扫描子目录** | 高 | 已实现 | `core/controller.py` 用 `os.walk`/`rglob` |
| **缓存机制** | 中 | 已实现 | `utils/cache.py` 有 `LRUCache` + `TimedLRUCache`；`chem/openbabel_utils/_cache.py` 另有专用缓存（共 16 文件）|
| **并行计算** | 中 | 已实现 | `utils/concurrency.py` + `core/task_manager.py` |
| **计算任务队列** | 高 | 已实现 | `core/task_manager.py`、`chem/psi4/_worker.py`（16 文件相关）|
| **暗色主题** | 高 | 已实现且可切换 | `ui/ui_builder/_theme.py` 的 `toggle_theme()`，命令面板有"切换主题（深/浅）" |
| **指纹类型扩展** | 高 | 已支持多种 | `_search.py`: `SUPPORTED_FP_TYPES = (FP2, FP3, FP4, MACCS, maccs, EState, estate)` |
| **子结构/相似性搜索** | 高 | 已实现 | `substructure_search()`、`similarity_search()`、`tanimoto()` |
| **统计仪表板** | 中 | 已实现 | `ui/dialogs/analytics_dialog.py` |
| **通知系统** | 低 | 已实现 | 6 个文件含 notify/toast 逻辑 |

---

## 三、⚠️ 部分实现（增强即可，非从零）

| 指南建议 | 现状 | 差距 |
|---------|------|------|
| **数据库索引优化** | 仅 2 个索引：`idx_calc_molecule`、`idx_calc_type`（都在 `calc_result` 表）| ❗ 指南点名的 `name`/`smiles`/`inchikey`/`tags` **均无索引** —— 这是真瓶颈 |
| **扩展格式支持** | `get_supported_formats()` 动态返回 OpenBabel 全部格式 | 装了 OB 后 cif/pdb/mol2/pdbqt 自动可用；**真正缺的是 `.gjf`/`.inp` 计算输入文件的解析器**（不是转换格式）|
| **大数据集支持** | `ui/app_helpers.py` 已有分批插入（每批 200 行 + `after_idle` 让出事件循环）| 非虚拟滚动，10,000+ 规模首屏仍偏慢，但已可用 |
| **API 端点扩展** | 已有 6 个端点（下详）| 缺 `/convert`、`/animate`、`/calculate` |

**现有 API 端点**（`api/server.py`）：

| 方法 | 端点 | 功能 |
|------|------|------|
| GET | `/health` | 健康检查（含依赖探测）|
| POST | `/inchikey` | SMILES → InChIKey |
| POST | `/descriptors` | 描述符计算 |
| POST | `/substructure` | 子结构搜索 |
| POST | `/similarity` | 相似性搜索 |
| POST | `/chem_query` | 化学查询 |

---

## 四、❌ 真实缺口（代码中零命中）

| 指南建议 | 优先级 | 核查结果 | 我的建议方案 |
|---------|--------|---------|------------|
| **3D 结构预览** | 高 | 0 命中（无 py3dmol/nglview/ThreeJS）| **用 matplotlib**（项目已是依赖）+ `mplot3d` 自绘球棍模型，`TkAgg` 后端与 tkinter 天然集成，**零新增依赖**，且自带鼠标旋转/缩放 |
| **多语言 i18n** | 高 | 0 命中（无 gettext/locale）| 需先做文案抽取层，工作量大，建议单独立项 |
| **工作流编辑器** | 中 | 0 命中 | 远期项，优先级建议下调 |
| **插件系统** | 低 | 0 命中 | 远期项 |

---

## 五、🔶 依赖已有设施（装依赖即自动获得）

| 指南建议 | 说明 |
|---------|------|
| **扩展格式支持** | OpenBabel 原生支持 100+ 格式，`get_supported_formats()` 会动态返回。当前沙盒无 OB 故只显示基础格式，**conda 环境装好后自动全支持** |
| **描述符扩展** | OB 的 `calculate_descriptors` 已封装，装 OB 后可覆盖拓扑/电荷类描述符 |

---

## 六、我的优先级建议（按 ROI 排序）

### 🥇 第一梯队：低成本高收益（建议立即做）

| 项目 | 预估成本 | 收益 |
|------|---------|------|
| **1. DB 索引补全** | 极小（~10 行 SQL）| 给 `molecule` 表的 `name`/`smiles`/`inchikey`/`tags` 建索引，直接解决大数据集查询慢——这正是指南"数据库优化"的核心诉求 |
| **2. API 补 3 端点** | 小（复用现有 6 端点框架）| `/convert`（转换）、`/descriptors` 已有了 → 主要补 `/animate`、`/calculate`，让 API 从"查询"升级为"可用服务" |
| **3. 3D 结构预览（matplotlib 方案）** | 中 | 指南列为高优先级且是真空缺；用 matplotlib 零新增依赖 |

### 🥈 第二梯队：中等投入（第一梯队完成后）

4. **标签与分类系统** —— 数据模型需加 `tags` 字段 + 迁移 + UI
5. **计算失败诊断** —— `utils/calc_log_parser.py` 已有解析基础，增强错误模式库 + 中文建议
6. **项目快照/版本管理** —— 复用 SQLite，做状态快照表

### 🥉 第三梯队：建议下调优先级

- **多语言 i18n**：全量 UI 文案改造，成本高，除非有明确英文用户需求
- **Web 版本 / 插件系统 / 工作流编辑器**：架构级改造，与桌面端并行维护成本高
- **ADMET 预测 / AI 逆合成**：依赖外部 API 和账号，且非核心场景

---

## 七、与指南路线图的差异说明

指南「第一阶段」列的 5 项中：

| 指南第一阶段项 | 我的判断 |
|--------------|---------|
| 批量文件导入增强（拖拽+递归）| ✅ **已完成**，可跳过 |
| 扩展格式支持（xyz/gjf/inp）| 🔶 xyz 已有；gjf/inp 需写**解析器**（非格式转换），成本被低估 |
| 暗色主题 | ✅ **已完成**，可跳过 |
| 统计仪表板 | ✅ **已完成**，可跳过 |
| 数据库索引优化 | ⚠️ **确实是真缺口**，但只需补 4 个索引，半天工作量 |

**即：指南第一阶段约 60% 的工作实际已完成，剩下的是「4 个索引」这个小活 + 「gjf/inp 解析器」。**

建议以本评估为准重排优先级，避免把时间花在已完成的功能上。
